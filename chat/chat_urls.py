from django.urls import path
from chat import api_views

urlpatterns = [

    # Defined URL
    # path('wallet/v1/savingplan',
    #      api_views.SavingPlanList.as_view(), name="SavingPlanList"),
  
]
